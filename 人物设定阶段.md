## 人物设定阶段

此阶段承接世界设定完成后，玩家为故事创建主角。系统保持与上一阶段相同的简化逻辑：历史记录仅保存用户手动输入与LLM输出，不涉及meta/normal模式切换，关键设定在触发历史总结指令时才发送给LLM。

---

### 1. 进入人物设定

用户点击对话区下方的 `<开启人物设定>` 按钮。

[系统输出：人物设定阶段。]

[系统在前端对话区输出一条固定指引，无需调用 LLM]：

> “接下里请为你的故事设定主角。你可以直接描述，比如‘一个退伍老兵，沉默寡言但心地善良’，或‘一个好奇心过重的医学院学生’。我会为你生成一份详细的人物档案。调整满意后请点击‘保存主角设定’按钮。”

此时对话区仅显示上述指引，等待用户输入。

---

### 2. 初次生成主角设定

用户输入关键词描述，例如：“一个退伍老兵，沉默寡言但心地善良，40岁左右”，并按下发送。

[系统组装 LLM 输入]

#### LLM 输入 Schema

*系统辅助性指令（固定，但含世界观描述）*  
*输出格式要求（固定，含人物档案生成规则）*  
*历史记录* （初次为空）  
*用户当前 prompt*

> **Note – LLM 实际接收的文字输入示例：**
> 
> ```
> 我在尝试一种新型的AI跑团，旨在通过结合AI创作与跑团游戏元素，
> 创造出文学性与娱乐性并重的RPG体验。请你根据CoC7th规则，扮演KP，
> 辅助我完成跑团。现在我们要创建主角的人物设定。
> 世界观描述如下：...（调用数据库）
> 输出格式要求：
> 将人物档案放置于<character_card> 与 </character_card> 标签之间，标签之间不可以有无关内容，包含且仅包含以下字段：
> 姓名、年龄、性别、职业、性格、人物肖像与重要经历，力量/敏捷/体型/外貌/体质/智力/意志/教育8大属性，
> HP，SAN，信用评级，8个本职技能+4个非本职技能，随身物品。
> 每个字段使用“字段名：内容”格式，同一行内的不同字段用两个空格隔开。
> 姓名/年龄/性别/职业共同占一段；
> 性格单独占一段；
> 人物肖像与重要经历单独占一段；
> 8大属性共同占一段；
> HP，SAN，信用评级共同占一段；
> 12个技能共同占一段；
> 随身物品单独占一段。
> 
> 以下为人物档案的内容生成规则限制：
> 1. 所有内容根据我的描述/世界观/CoC7th规则合理补全，属性与技能要符合职业等人物设定
> 2. 属性点总和为460，单项属性在15-90之间
> 3. HP=（体质+体型）%10，SAN=意志
> 4. 各类技能（包括信用评级）的点数计算规则为基础+职业+兴趣，基础值固定，职业技能点只能分配给本职技能和信用评级，兴趣技能点可以分配给所有技能（信用评级除外）
> 5. 信用评级基础值为0，代表财富与社会地位，要符合职业设定，且占用职业技能点
> 6. 单项本职技能点数不超过80，非本职技能不超过50
> 7. 所有职业的基础数值由时代背景下的普通人样貌决定，与角色本身无关；
> 基础数值有4档，0为克苏鲁神话和信用评级，1为普通人一般接触不到的技能（如唇语/催眠），
> 10为普通人偶尔用到/一般熟悉的技能（如心理学/博物学），25为生活技能或生物本能（如格斗/聆听）
> 8. 职业技能点总和=教育*4，教育*2+力量*2等，根据职业决定计算方式
> 9. 兴趣技能点总和=智力*2
> 10. 核查数值计算，注意不要把技能基础点数计入职业技能点与兴趣技能点总和的限制。
> 
> 以下为我的人物描述：
> 一个退伍老兵，沉默寡言但心地善良，40岁左右
> ```

[系统调用 LLM API，stream 方式将生成内容展示在对话区]

Note: AI在后续跑团对话中只需要记住8个本职技能和4个非本职技能，当需要使用超出这些技能范围外的技能进行判定时，按照时代背景常理生成技能基础数值并根据基础数值来判定即可；系统在存入主角设定时，在技能部分自动补充：”其余技能：根据时代背景常理自动生成基础数值，需要时据此判定“

---

### 3. 调整与迭代

用户阅读后，可以在对话框中继续提出修改意见，如：“背景故事太苦了，让他有个女儿吧，是他活下去的唯一动力。”

[系统重新组装输入，此次输入中包含之前的对话历史]

#### LLM 输入 Schema（调整轮次）

*系统指令（固定，含世界观描述）*  
*输出格式要求（固定，含人物档案生成规则）*  
*历史记录* （仅包含用户**手动输入**的prompt和LLM输出）  
*用户当前 prompt*

> **Note – 调整轮次 LLM 接收文字输入示例：**
> 
> ```
> 我在尝试一种新型的AI跑团，旨在通过结合AI创作与跑团游戏元素，
> 创造出文学性与娱乐性并重的RPG体验。请你根据CoC7th规则，扮演KP，
> 辅助我完成跑团。现在我们要创建主角的人物设定。
> 世界观描述如下：...（调用数据库）
> 输出格式要求：...
> 
> 我们的对话历史如下，最后一行是我的最新prompt：
> Player: 一个退伍老兵，沉默寡言但心地善良，40岁左右
> KP: <character_card>……（完整人物档案）</character_card>
> Player: 背景故事太苦了，让他有个女儿吧，是他活下去的唯一动力。
> ```

[LLM 生成新版本人物档案全文并展示]

用户可多次重复此步骤，直至满意。

---

### 4. 存档主角设定

用户对当前对话中最后一条人物档案满意后，点击对话区下方的 `<保存主角设定>` 按钮。

[系统提取LLM最新输出中 `<character_card>` 标签之间的文本]

[系统将提取的文本自动填入UI右侧 `<主角设定>` 模块，已有则覆盖，并自动存入`protagonist` 字段]

[系统在对话区提示：“主角设定已保存，你可以在侧边栏随时查看，支持手动修改，并在点击`<保存修改>`后自动存档；如果不需要再调整，请点击 `<故事开幕>` ，开始冒险。”]

用户可以实时修改`<主角设定>`模块中的内容，系统在其点击位于模块下方的`<保存修改>`后覆盖写入数据库。

[同时，`Game Orchestrator` 会将该人物档案文本标记为当前会话的活跃设定。在后续对话中，重新输出给LLM，使其长期记忆该设定。]

---

### 前端设计总结

- `<开启人物设定>` 按钮：进入阶段，显示固定指引
- `<保存主角设定>` 按钮：提取 `<character_card>` 内容，覆盖写入数据库
- `<主角设定>` 存储模块：右侧面板显示，支持手动编辑
- `<保存修改>`按钮：位于`<主角设定>`的底部，点击后将主角设定覆盖写入数据库
- `<故事开幕>` 按钮：结束设定阶段，进入故事开幕

### 数据库设计总结

`protagonist` 字段：存储主角最新档案；系统在存入主角设定时，在技能部分自动补充：”其余技能：根据时代背景常理自动生成基础数值，需要时据此判定“
