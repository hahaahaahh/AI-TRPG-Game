## 世界设定阶段

---

### 1. 进入世界设定

用户点击主页的 `<开启故事设定>` 按钮。

[系统输出：世界设定阶段。]

[系统在前端对话区输出一条固定指引，无需调用 LLM]：

> “请用几句话描述你希望的世界类型、风格或核心冲突。你可以说‘一个蒸汽朋克风的低魔世界’，或‘大航海时代背景下的克苏鲁神话’，然后我会为你扩写成完整的世界观与背景。调整好世界观后请点击'保存世界设定'按钮。”

此时对话区仅显示上述指引，等待用户输入。

---

### 2. 初次生成世界观

用户输入关键词描述，例如：“蒸汽朋克与古老魔法并存，科技与信仰冲突剧烈的大陆”，并按下发送。

[系统组装 LLM 输入]

#### LLM 输入 Schema

*系统辅助性指令（固定）*  
*输出格式要求（固定）*  
*历史记录* （初次为空）  
*用户当前 prompt*

> **Note – LLM 实际接收的文字输入示例：**
> 
> ```
> 我在尝试一种新型的AI跑团，旨在通过结合AI创作与跑团游戏元素，
> 创造出文学性与娱乐性并重的RPG体验。请你根据CoC7th规则，扮演kp，辅助我完成跑团。
> 现在我们先创建世界，根据我输入的文段生成文风恰切、有代入感的世界观描述。世界观描述
> 段落的输出格式要求：300字以内，将世界观描述内容放置于 <world_description> 与 </world_description> 标签之间，
> 标签之间不可以有无关内容。
> 
> 以下为我的输入文段：
> 蒸汽朋克与古老魔法并存，科技与信仰冲突剧烈的大陆
> ```

[系统调用 LLM API，stream 方式将生成内容展示在对话区]

---

### 3. 调整与迭代

用户阅读后，可以在对话框中继续提出修改意见，如：“魔法系统太弱了，我希望魔法是主流，科技反而被压制。”

[系统重新组装输入，此次输入中包含之前的对话历史]

#### LLM 输入 Schema（调整轮次）

*系统指令（固定）*    
*输出格式要求（固定）*  
*历史记录* （仅包含用户**手动输入**的prompt和LLM输出）  
*用户当前 prompt*

**Note – LLM 实际接收的文字输入示例：**

```
我在尝试一种新型的AI跑团，旨在通过结合AI创作与跑团游戏元素，
创造出文学性与娱乐性并重的RPG体验。请你根据CoC7th规则，扮演kp，辅助我完成跑团。
现在我们先创建世界，根据我输入的文段生成文风恰切、有代入感的世界观描述。世界观描述
段落的输出格式要求：300字以内，将世界观描述内容放置于 <world_description> 与 </world_description> 标签之间，
标签之间不可以有无关内容。

以下为我们的对话历史：
Player：蒸汽朋克与古老魔法并存，科技与信仰冲突剧烈的大陆
KP:...
Player：...
```

[LLM 生成新版本世界观全文并展示]

用户可多次重复此步骤，直至满意。

---

### 4. 存档世界观

用户对当前对话中最后一条世界观描述满意后，点击对话区下方的 `<存档当前世界观>` 按钮。

[系统提取LLM最新输出中的相关标签之间的文本，填入右侧 `<世界观与背景>` 编辑模块，已有则覆盖，并自动存入`world_settings` 字段]

[系统在对话区提示：“世界观已保存，你可以在侧边栏随时查看。如果不需要再调整，请点击`<开启人物设定>`按钮，进入下一阶段。”]

[同时，`Game Orchestrator` 会将该世界观文本标记为当前会话的活跃设定。在后续对话中，重新输出给LLM，使其长期记忆该设定。]

---

### 前端设计总结

`<开启故事设定>` 按钮，`<存档当前世界观>`按钮，`<世界观与背景>`存储模块。

### 数据库设计总结

`world_settings`字段：存储世界观与背景
