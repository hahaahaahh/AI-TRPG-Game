## 故事开幕与正常叙述

此阶段在主角设定保存后启动，故事正式展开。系统保持简化逻辑，不涉及meta/normal模式切换。世界观、主角设定持续输入给LLM；其余设定（地点/NPC/物品等）仅在触发历史总结时作为必要设定输入给LLM。

---

### 1. 进入故事开幕

用户点击对话区下方的 `<故事开幕>` 按钮。

[系统锁定 `<故事开幕>` 按钮，使其不可再次点击。]

[系统输出：“故事即将开幕。”]

[系统组装 LLM 输入]

#### LLM 输入 Schema（故事开幕）

*系统辅助性指令（固定）*
*输出格式要求（固定，仅限故事开幕）*
*历史记录* （世界观描述全文 + 主角设定全文）

> **Note – LLM 实际接收的文字输入示例：**
> 
> ```
> 我在尝试一种新型的AI跑团，旨在通过结合AI创作与跑团游戏元素，
> 创造出文学性与娱乐性并重的RPG体验。请你根据CoC7th规则，扮演KP，
> 辅助我完成跑团。现在，请为我撰写一个符合设定、有代入感的跑团故事开幕。
> 
> 世界观与人物设定如下：
> 世界观描述：蒸汽朋克与古老魔法并存，科技与信仰冲突剧烈的大陆……
> 主角设定：姓名：赵铁生，年龄：42岁，职业：退伍军人，现为码头工人……
> 
> 故事开幕的输出格式要求：
> 1.正常叙述，用 <narration> 与 </narration> 包裹
> 2.模仿RPG游戏模式，提取叙述中的重要地点、NPC、物品，也用标签包裹
>   地点：<location>名称/指代：描述</location>，
>   NPC：<npc>名称/指代：描述</npc>，
>   物品：<item>名称/指代：已获得/已失去，描述</item>，
>   NPC需为新出场人物或有重要状态更新的旧人物；
>   物品需为主角已获得的新物品，或失去的旧物品，并标明状态。
>   对于格式2，若无相应内容，可忽略相应标签。
> 3.主角活动选项，用 <option> 与 </option> 包裹，标签内格式为：
>   A. 选项内容
>   B. 选项内容
>   C. 选项内容
>   D. 自由行动
> 4.标签之间不得有无关内容。
> ```

[系统调用 LLM API，输入内容。]

[系统扫描LLM输出：]

- 将 `<narration>` 标签内的文本存入数据库 `chat_record` 字段。
- 将 `<location>` 标签内的内容逐个解析，追加存入数据库 `location` 字段，同时填入UI右侧 `<重要地点>` 模块，同名直接覆盖。
- 将 `<npc>` 标签内的内容逐个解析，追加存入数据库 `npc` 字段，同时填入UI右侧 `<重要npc>` 模块，同名直接覆盖。
- 将 `<item>` 标签内的内容逐个解析，追加存入数据库 `inventory` 字段，同时填入UI右侧 `<物品仓库>` 模块，同名直接覆盖。
- 将 `<option>` 标签内的文本存入数据库 `option_buffer` 字段，覆盖原有内容。

[系统将LLM本次的完整输出显示在对话框。]

[系统在用户输入区上方渲染选项按钮：`<A> <B> <C> <D>`，仅显示字母。]

[用户点击选项按钮的行为规则：]

- 点击任意按钮，输入区自动填入“选项X”（X为对应字母）。
- 允许点击多个按钮，如依次点击A和B，输入区显示“选项A和B”。
- 再次点击已选中按钮可取消，输入区内容实时刷新。
- D选项允许用户在“选项D”后继续输入自由文字描述，提示允许基于A/B/C修改
- 用户可在输入区直接手动修改选项文字，以最后一次发送内容为准。

[用户点击对话发送按钮后，系统进入正常叙述循环。]

**Note: 后续可引入“召集小队成员”的固定选项。**

---

### 2. 正常叙述阶段

[系统将用户输入的“选项X”替换为选项对应的具体文本内容，与用户附加的自由文字（如有）拼接，作为本轮 `用户当前prompt`，并存入数据库 `chat_record` 字段。]

[系统组装LLM输入。]

#### LLM 输入 Schema（叙述I —— 等待判定或叙事）

*系统辅助性指令（固定）*
*输出格式要求（固定，含叙述、更新标签和选项）*
*历史记录* （仅含用户手动输入的prompt，和 `chat_record` 字段中的LLM输出；超过长度阈值时替换为历史总结）
*用户当前 prompt*

> **Note – 叙述I LLM 接收文字输入示例：**
> 
> ```
> 我在尝试一种新型的AI跑团……现在，你作为KP，需要根据玩家行为推进剧情。
> 
> 世界观与主角设定如下：
> 世界观描述：蒸汽朋克与古老魔法并存，科技与信仰冲突剧烈的大陆……
> 主角设定：姓名：赵铁生，年龄：42岁，职业：退伍军人，现为码头工人……
> 
> 历史记录如下：
> [故事开幕叙述] 夜幕低垂，码头区弥漫着咸腥的海风。赵铁生拖着疲惫的身躯……（完整开幕文本）
> 
> Player：我环顾四周，看看有没有什么可疑的人。
> 
> 规则：
> 若衍生剧情有投掷判定环节，你需要基于主角设定和世界观，思考用于判定的技能/属性，
> 并在<dice></dice>标签中填入用于投掷的骰子，多个则用英文逗号连接，如投掷一次百面骰，两次四面骰：
> <dice> 1d100, 2d4 </dice>。
> 
> 如果本轮输出中包含 <dice> 标签，忽略以下所有输出格式要求。
> 如果本轮输出中不包含<dice> 标签，则必须按以下格式输出：
> 1.正常叙述，用 <narration> 与 </narration> 包裹
> 2.模仿RPG游戏模式，提取叙述中的重要地点、NPC、物品，也用标签包裹
>   地点：<location>名称/指代：描述</location>，
>   NPC：<npc>名称/指代：描述</npc>，
>   物品：<item>名称/指代：已获得/已失去，描述</item>，
>   NPC需为新出场人物或有重要状态更新的旧人物；
>   物品需为主角已获得的新物品，或失去的旧物品，并标明状态。
>   对于格式2，合理叙事中若无相应内容，忽略相应标签，不要为了填充而编造内容。
> 3.主角HP/SAN更新，如有变化，格式为：<HP>新数值</HP>，<SAN>新数值</SAN>
>   若无变化，忽略此标签。
> 4.主角活动选项，用 <option> 与 </option> 包裹，标签内格式为：
>   A. 选项内容
>   B. 选项内容
>   C. 选项内容
>   D. 自由行动
> 5.标签之间不得有无关内容。
> ```

[系统将LLM输出存入 `chat_record` 字段。]

[系统扫描LLM本次输出。]

---

#### 分支A：无 `<dice>` 标签

[系统执行以下处理（部分内容同开幕阶段）：]

- 将 `<narration>` 标签内的文本存入数据库 `chat_record` 字段。
- 将 `<location>`、`<npc>`、`<item>` 标签内的内容分别解析，追加存入对应数据库字段，并更新UI侧边栏模块。
- 若有 `<HP>` 或 `<SAN>` 标签，解析数值并在数据库/UI更新主角对应属性。
- 将 `<option>` 标签内的文本存入数据库 `option_buffer` 字段，覆盖原有内容。

[系统将LLM输出显示在对话框，渲染选项按钮 `<A> <B> <C> <D>`。]

[等待用户输入，用户发送后系统按“叙述I”格式重新组装输入。]

---

#### 分支B：有 `<dice>` 标签

[系统解析 `<dice>` 标签内的骰子指令，按顺序调用本地真随机数生成程序，记录每次投掷的结果数字。]

[系统将LLM本次输出（含 `<dice>` 标签）显示在对话框。]

[系统将投掷结果以独立消息形式显示在对话框，格式为：“【系统投掷结果】50, 30”。]

[显示投掷结果期间，暂时禁用用户输入权限，直至LLM输出下一个无<dice>标签的文段]

[系统自动按“叙述II”格式组装LLM输入。]

---

#### LLM 输入 Schema（叙述II —— 接收判定结果后叙事）

*系统辅助性指令（固定）*
*输出格式要求（固定，含叙述、更新标签和选项）*
*历史记录* （同叙述I）
*判定结果*

> **Note – 叙述II LLM 接收文字输入示例：**
> 
> ```
> 我在尝试一种新型的AI跑团……现在，你作为KP，需要根据投掷结果推进剧情。
> 
> 世界观与主角设定如下：……
> 
> 历史记录如下：……
> 
> 投掷结果依次为：50，30
> 
> 规则：
> 若衍生剧情仍有投掷判定环节，你需要基于主角设定和世界观，思考用于判定的技能/属性，
> 并在<dice></dice>标签中填入用于投掷的骰子，多个则用英文逗号连接，如投掷一次百面骰，两次四面骰：
> <dice> 1d100, 2d4 </dice>。
> 
> 如果本轮输出中包含 <dice> 标签，忽略以下所有输出格式要求。
> 如果本轮输出中不包含<dice> 标签，则必须按以下格式输出：
> 1.正常叙述，用 <narration> 与 </narration> 包裹
> 2.模仿RPG游戏模式，提取叙述中的重要地点、NPC、物品，也用标签包裹
>   地点：<location>名称/指代：描述</location>，
>   NPC：<npc>名称/指代：描述</npc>，
>   物品：<item>名称/指代：已获得/已失去，描述</item>，
>   NPC需为新出场人物或有重要状态更新的旧人物；
>   物品需为主角已获得的新物品，或失去的旧物品，并标明状态。
>   对于格式2，合理叙事中若无相应内容，忽略相应标签，不要为了填充而编造内容。
> 3.主角HP/SAN更新，如有变化，格式为：<HP>新数值</HP>，<SAN>新数值</SAN>
>   若无变化，忽略此标签。
> 4.主角活动选项，用 <option> 与 </option> 包裹，标签内格式为：
>   A. 选项内容
>   B. 选项内容
>   C. 选项内容
>   D. 自由行动
> 5.标签之间不得有无关内容。
> ```

[系统接收LLM输出后，根据有无<dice>标签，选择执行分支A或分支B。]

---

### 3. 对话历史总结机制

触发时机：系统将最新LLM输出存入 `chat_record` 字段之后，用户输入下一轮prompt之前。

[触发期间，系统禁止用户输入，并在对话区显示提示：“系统正在更新记忆，请稍等片刻。”]

触发条件：`chat_record` 中除最近一条记录外的历史内容，预估总字数超过系统设定阈值。

[系统暂停正常叙述，向 LLM 发送总结指令。]

#### LLM 输入 Schema（历史总结）

*系统总结指令（固定）*
*历史记录* （当前 `chat_record` 中除最新一条外的完整内容）
*必要设定* （数据库中的世界观、主角设定、已解锁地点/NPC/物品等）

> **Note – 历史总结 LLM 接收文字输入示例：**
> 
> ```
> 你是跑团KP，现在需要帮我总结迄今剧情。你给出的总结要保证自己后续
> 可以通过该总结正常推进跑团进程，保证故事的合理性，暗示故事可能的伏笔。
> 
> 总结的输出格式要求：
> 用 <summary> 与 </summary> 标签包裹总结内容，标签之间不得有无关内容。
> 字数控制在300字以内。
> 
> 我们的对话记录如下：
> [近x轮对话原文，已被替换为历史总结的部分正常视为原文，即接受根据往期总结再生成新总结]
> 
> 故事必要设定如下：
> 世界观：蒸汽朋克与古老魔法并存，科技与信仰冲突剧烈的大陆……
> 主角：赵铁生，42岁，退伍军人，码头工人……
> 已解锁地点：海港城码头区、老城区酒馆……
> 已知NPC：老乞丐（无害，交谈过一次），深色风衣男人（身份未知，跟踪中）……
> 主角物品栏：一把生锈的匕首
> ```

[系统接收到含 `<summary>` 标签的LLM输出后，执行记忆替换：]

- 保留 `chat_record` 中最新一条记录（即最近一轮的LLM输出），删除其余所有旧记录。
- 将 `<summary>` 标签内的总结文本插入到 `chat_record` 的最前端。

[替换完成后，系统恢复用户输入权限，并在对话区显示提示：“系统已完成自动记忆更新。”]

若此时处于投掷判定流程中（即系统刚返回投掷结果，等待向LLM输入时触发了总结），总结完成后系统自动继续发送判定结果，流程不受中断。

---

### 前端设计总结

- `<故事开幕>` 按钮：仅可点击一次，点击后锁定变灰。
- 叙事区：流式展示 `<narration>` 标签，和`<option>` 标签内的内容（去掉标签本身），渲染为沉浸式气泡。
- 选项区：自动将 `<option>` 生成可点击按钮 `<A> <B> <C> <D>`（仅需显示字母），互动效果参前。
- 侧边栏模块：实时展示已解锁地点、已知NPC、物品仓库的更新，内容从数据库读取。
- 投掷结果：以独立系统消息形式展示在对话区。

### 数据库设计总结

| 字段               | 用途                     | 更新方式                    |
|:---------------- |:---------------------- |:----------------------- |
| `chat_record`    | 存储对话历史（LLM输出与用户prompt） | 每轮追加；总结时替换旧内容           |
| `option_buffer`  | 暂存当前轮次选项文本             | 每轮LLM输出后覆盖              |
| `location`       | 已解锁重要地点列表              | LLM输出含 `<location>` 时追加 |
| `npc`            | 已知NPC列表及状态             | LLM输出含 `<npc>` 时追加或更新   |
| `inventory`      | 主角物品仓库                 | LLM输出含 `<item>` 时追加或更新  |
| `protagonist`    | 主角完整设定（含技能）            | 人物设定阶段写入，可手动修改          |
| `world_settings` | 世界观设定                  | 世界设定阶段写入，理论上不再修改        |


